# CareerPilot AI 项目审计报告（Phase 0）

> 审计日期：2026-09-15  
> 审计目标：面向求职简历与面试展示，确认项目真实能力、技术债、功能取舍和后续重构顺序。  
> 本阶段只做审计与基线验证，没有修改业务代码、数据库结构或运行数据。

## 1. 结论先行

CareerPilot AI 不是一个只有页面壳子的课程项目。它已经具备较完整的前后端分层、简历文件处理、人岗匹配、LangGraph 工作流、RAG 检索、引用溯源、任务状态持久化、权限校验以及较多自动化测试。最适合作为简历项目的主线是：

**简历解析 → 职位录入 → 可解释人岗匹配 → 风险与差距分析 → 带引用的职业问答 → 求职进度管理。**

当前最大问题不是“功能太少”，而是功能面过宽、AI 接入分散、面试模块存在两套链路、部分 UI 只是占位、项目无法在干净环境中稳定启动。这些问题会让面试官难以在短时间内理解项目真正的技术亮点。

建议把项目收敛为一个“可解释、可追踪、可评估的 AI 求职决策系统”，而不是继续保留学习计划、求职信、复杂爬取和两套面试等相互竞争的功能。

## 2. 当前项目结构与技术栈

| 层级 | 当前实现 | 评价 |
| --- | --- | --- |
| 前端 | Vue 3、TypeScript、Vite、Pinia、Vue Router、Axios、Tailwind CSS、ECharts | 技术栈合理，API、类型、页面、组件和状态层已有分离 |
| 后端 | FastAPI、Pydantic、SQLAlchemy、Alembic、SQLite | 分层较完整，适合作为单体应用演示 |
| AI 编排 | LangGraph | 用于匹配任务和职业助手的可恢复节点式工作流 |
| RAG | Sentence Transformers、FAISS、LangChain 文档与向量存储能力 | 已有知识库和个人上下文两类检索，可输出引用 |
| 模型接入 | Dify 多应用接口、OpenAI 兼容接口、Mock/Fake Provider | 抽象基础存在，但供应商和密钥数量过多 |
| 文件处理 | PDF、DOCX 提取，文件签名/MIME/压缩包安全校验 | 是项目中值得展示的工程化能力 |
| 测试 | Pytest、Vitest、Playwright | 测试数量可观，但后端环境和 E2E 启动目前不可复现 |
| 数据存储 | SQLite、上传文件目录、FAISS 索引 | 本地演示足够，但需要脱敏、初始化和环境隔离 |
| 启动方式 | PowerShell 脚本、Alembic、Uvicorn、Vite | 偏 Windows 本机，缺少完整安装与跨平台入口 |

主要目录职责：

```text
backend/
  app/api/                 FastAPI 路由
  app/services/            业务服务与工作流入口
  app/repositories/        数据访问层
  app/models/              SQLAlchemy 模型
  app/schemas/             Pydantic 输入输出模型
  app/ai/                  通用 AI Provider
  app/integrations/dify/   Dify 各功能 Provider
  app/matching/            匹配规则和评分能力
  app/semantic/            语义检索能力
  app/job_sources/         职位源适配器
  evaluation/matching/     现有匹配评测集和报告
  tests/                   后端测试

frontend/
  src/api/                 HTTP 接口封装
  src/types/               TypeScript 类型
  src/pages/               业务页面
  src/components/          通用与领域组件
  src/stores/              Pinia 状态
  src/router/              页面路由和权限控制
  tests/e2e/               Playwright 端到端测试

data/
  careerpilot.db           当前本地数据库
  uploads/                 用户上传文件
  faiss/                   向量索引

scripts/
  start-local.ps1          本地启动
  start-demo.ps1           演示数据启动
  reset-demo.ps1           演示数据重置
```

## 3. 当前能力审计与取舍

### 3.1 建议保留并强化的主线

| 模块 | 当前状态 | 证据与价值 | 决策 |
| --- | --- | --- | --- |
| 用户认证与权限 | 已实现 | JWT access/refresh、Argon2、资源归属校验、短期 SSE ticket | 保留；补充 README 和安全说明 |
| 简历中心 | 已实现 | PDF/DOCX 上传、文本提取、结构化解析、重试、人工确认、版本和证据校验 | 核心保留；统一 AI Provider，完善解析评测 |
| 职位管理 | 已实现但过宽 | 有职位、技能、收藏、手工/CSV/网页/API 等多种来源 | 保留职位核心；收敛为手工粘贴 JD、CSV 导入和少量内置示例 |
| 人岗匹配 | 已实现 | LangGraph、确定性规则、语义召回、阻断风险、持久化报告、人工复核、SSE | 第一核心亮点；重点补评测、解释和可观测性 |
| 职业助手 | 已实现 | 知识库 FAISS、个人 SQL 上下文、引用、无证据拒答、工作流运行记录 | 第二核心亮点；统一模型层并补调用指标 |
| 求职看板 | 已实现 | 基于简历、匹配和申请漏斗汇总 | 保留；增加 AI 运行质量指标和演示引导 |
| 求职申请 Kanban | 已实现 | CRUD、状态迁移和历史记录 | 保留但简化状态，避免像一个独立 ATS 产品 |
| 知识库管理 | 已实现 | 文档入库、索引、检索，是职业助手 RAG 的数据基础 | 管理端保留；降低入口曝光，作为支撑能力 |

匹配工作流当前已经包含以下节点，说明它并非一次简单的 LLM 调用：

```text
load_inputs
  → deterministic_matching
  → semantic_retrieval
  → blocking_risk_check
  → save_report
  → human_review
```

默认匹配逻辑同时考虑硬技能、证据、经验/教育、语言/地点/资格、用户偏好和其他条件，并使用确定性与语义得分的混合策略。这部分应当成为简历描述和面试演示的中心。

### 3.2 建议删除、隐藏或降级的功能

| 模块 | 问题 | 决策 | 删除前依赖处理 |
| --- | --- | --- | --- |
| 学习计划 | 与“求职决策主线”关联弱，又需要独立 Dify 应用和任务模型 | 删除前端入口、API 和生成链路 | 职业助手的个人上下文目前会读取学习计划，需先解除该依赖；数据库表可暂时保留，避免首轮破坏性迁移 |
| 求职信生成 | 与简历优化职责重叠，新增一套 Provider 和测试面 | 删除 | `generated_materials` 与简历优化共享，不能整表删除；只移除 cover-letter 类型和关联代码 |
| 旧版面试流程 | 与聊天式面试重复，路由虽重定向但页面、API、服务和测试仍保留 | 删除旧版 create/answer/report 链路 | 保留聊天式链路前，先验证真实模型和异常恢复；无法稳定时从导航隐藏 |
| 聊天式 AI 面试 | 实现复杂、Dify 依赖多，当前未在真实配置下验证 | 暂列可选能力 | Phase 3 做限时稳定性验证；不达标则隐藏，不作为核心承诺 |
| 复杂职位源管理 | HTML/API/单页 URL/HKMA 等适配器增加维护、合规和演示不确定性 | 移除管理端入口和自动同步，保留手工/CSV | `jobs.source_id` 与职位数据有关，不能直接删除表；先保留系统内置 source，再逐步简化 |
| 过细申请状态 | 目前有 SAVED、PREPARING、APPLIED、ASSESSMENT、INTERVIEW、OFFER、ACCEPTED、REJECTED、WITHDRAWN | 收敛 | 建议只展示 Saved、Applied、Interview、Offer、Rejected；旧数据需设计映射 |
| 全局搜索框 | 没有绑定状态、事件或搜索接口 | 删除或实现真正的跨模块搜索 | 简历项目无需为了视觉保留假交互，建议直接删除 |
| 通知按钮/红点 | 固定红点，提示却是“暂无新通知” | 删除 | 无后端通知系统，不应制造伪功能 |
| 账户设置 | 侧边栏明确标为 unavailable | 删除入口 | 账号基础能力留在认证即可 |

### 3.3 应保留但需要重构的功能

#### 简历优化

当前实现已经具备“基于匹配报告、禁止捏造、用户确认、生成新版本”等有价值的约束，因此不建议直接删除。它应被降级为职位详情或匹配报告中的可选动作，而不是独立产品线。后续需统一模型调用层、减少页面层级，并明确展示“建议修改”与“用户已采纳”的区别。

#### AI 可观测性

`ModelUsageLog` 已记录 provider、model、operation、输入/输出 token、延迟、估算成本、成功状态和错误信息；简历解析、简历优化、学习计划、求职信和面试的部分调用会写入该表。这说明基础设施已经存在，但还没有形成完整能力：

- 职业助手调用未形成一致的模型使用日志；
- Dify 工作流主要把尝试次数和延迟保存在运行状态中；
- Dashboard 没有展示请求数、成功率、平均/分位延迟、token 和失败原因；
- Mock、Fake、Dify、OpenAI-compatible 的字段语义没有统一。

建议下一阶段建立一个统一的 `AIProvider` 和调用埋点入口，所有 AI 功能都通过同一层记录指标。

## 4. AI、LangGraph、Dify 与 RAG 审计

### 4.1 当前 AI 接入

- 默认 `AI_PROVIDER=mock`，通用 OpenAI-compatible Provider 当前主要服务于简历解析，默认配置指向兼容接口。
- 默认 `DIFY_PROVIDER_MODE=fake`。
- Dify 被拆成学习计划、简历优化、知识问答、求职信、面试计划、面试回答、面试聊天、面试总结等多套应用和密钥。
- Embedding 默认使用 Sentence Transformers，也有 Fake Provider 供测试使用。

这种实现证明了项目能对接多种 AI 能力，但不适合简历展示：部署成本高、配置容易出错，也难以解释一个请求经过了哪套模型。

### 4.2 推荐目标

后续建议统一为：

```text
业务服务 / LangGraph 节点
          ↓
统一 AIProvider 接口
          ↓
OpenAI-compatible Provider / Mock Provider
          ↓
统一重试、超时、结构化输出校验、日志与成本统计
```

Dify 可保留为历史实现说明或可插拔 Provider，但不应再要求多个功能各配置一个应用密钥。核心简历解析、匹配说明和职业助手需要在单一兼容接口下可运行。

### 4.3 LangGraph 评价

匹配和职业助手都真实使用了节点图、运行记录和步骤状态，不属于为了写简历而空加依赖。应继续保留，但需要在 README 中解释：

- 为什么使用图工作流而不是一条 service 函数；
- 每个节点的输入、输出、失败重试和持久化边界；
- 哪些节点是确定性逻辑，哪些节点会调用模型；
- Mock 模式和真实模型模式的差异。

### 4.4 RAG 评价

职业助手已具备知识文档检索、个人数据检索、引用和证据不足时拒答，是较好的项目亮点。当前知识种子约有二十余份职业相关 DOCX，运行目录中已有 FAISS 索引。需要补足的不是更多文档，而是：

- 可复现的知识库初始化命令；
- 引用命中率、无证据拒答率和检索质量评测；
- 索引版本、文档版本和 embedding 模型信息；
- 对隐私数据与公共知识数据的清晰隔离。

## 5. 面试模块重复实现审计

后端 `interviews` API 同时暴露两组能力：

1. 旧版：创建面试、逐题提交回答、生成报告；
2. 新版：开始聊天、发送消息、完成/取消会话、查询状态和报告。

服务文件和 Dify interview provider 体积较大，同时维护了计划生成、答案评估、聊天和最终评估。前端虽然会把部分旧详情路由重定向到聊天页，但旧页面、报告页、API 方法、单元测试和 E2E 测试仍然存在。

结论：这是当前最明确的重复功能。Phase 2 应删除旧版链路；新版只在真实配置完成稳定性验证后继续对外展示。

## 6. 职位源与数据获取审计

现有适配器包括 MANUAL、CSV、GENERIC_HTML、REST_API、SINGLE_JOB_URL，以及 HKMA 等预设/公共来源，并配有管理端 CRUD、测试、预览和同步。

这是开发量较大的模块，但不是目标岗位作品集中最有说服力的部分，且存在页面结构变化、反爬、速率限制、来源授权和数据合规风险。建议保留：

- 手工粘贴职位描述；
- CSV 导入；
- 少量脱敏的内置演示职位。

建议移除或隐藏：

- 通用网页抓取配置；
- REST 外部源配置；
- 单职位 URL 自动解析；
- 定时同步和复杂的来源管理后台。

删除时必须保留职位实体、技能实体和一个系统内置来源，因为匹配、申请和演示数据都依赖职位记录。

## 7. 测试与可运行性基线

本次实际执行结果：

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 前端 TypeScript 检查 | 通过 | `npm run typecheck` |
| 前端 ESLint | 通过 | `npm run lint` |
| 前端 Vitest | 通过 | 30 个测试文件、144 个测试全部通过 |
| 后端 Pytest | 未执行 | 项目没有 `backend/.venv`，当前系统 Python 未安装 FastAPI、SQLAlchemy 等后端依赖 |
| Playwright E2E | 失败 | `npm run test:e2e` 在启动后端时因 `ModuleNotFoundError: No module named 'app'` 退出 |

E2E 失败的直接原因是 `frontend/scripts/run-e2e.mjs` 在 `backend` 目录中执行 `python scripts/run_e2e.py`，而该方式把 `backend/scripts` 而不是 `backend` 放进模块搜索路径，随后 `seed_e2e.py` 的 `from app import models` 无法导入。修复该入口后，还需要安装后端依赖才能继续验证。

测试资产本身较多：后端约 22 个测试文件，前端约 30 个单元测试文件和 14 个 E2E spec。但目前没有一条单独的、清晰可展示的主路径 E2E：

```text
注册/登录
→ 上传并确认简历
→ 粘贴或选择职位
→ 发起匹配
→ 查看解释和风险
→ 基于报告向职业助手提问
→ 将职位加入求职看板
```

Phase 4 应把这条路径做成一条确定、可重复、使用临时数据目录的 smoke/E2E 测试。

## 8. 现有匹配评测审计

`backend/evaluation/matching/` 已存在冻结的确定性基线、混合模式样例、标签、运行器、图表和 Markdown 报告。现有报告记录了 52 个确定性基线、10 个语义案例，并给出了推荐一致性、排序准确率和改进案例。这是可继续利用的资产。

但它还不能完全支撑简历中关于“AI 匹配质量”的陈述：

- 混合语义案例数量偏少；
- 尚未形成 30–50 组匿名简历—职位对的统一评测集；
- 缺少简历技能抽取准确率；
- 缺少职位 required skill 识别准确率；
- 缺少 blocking risk 召回率；
- 缺少结构化输出合法率；
- 缺少 Mock/真实模型或多次运行的一致性对比。

建议保留现有结果作为回归基线，不覆盖或夸大；新增评测集和指标后再更新 README 中的量化结论。

## 9. 配置、脚本与交付完整性

当前根目录缺少以下简历项目常见交付物：

- `README.md`；
- `.env.example`；
- 后端锁定依赖文件；
- Dockerfile / `docker-compose.yml`；
- CI 配置；
- License；
- 跨平台的初始化/启动入口。

当前 PowerShell 启动脚本要求开发者已经创建 `backend/.venv`、安装后端依赖并安装前端 `node_modules`，但没有提供从零安装流程。因此“干净环境一键启动”当前结论为 **不通过**。

现有配置默认使用 Mock/Fake 模式是正确方向，因为它允许没有外部密钥时进行演示。需要进一步把 Mock 与真实模式的启动方法、功能差异和所需变量写清楚，并增加启动前配置检查。

## 10. 数据与隐私风险

本地 `data/` 当前包含 SQLite 数据库、DOCX/PDF 上传文件、FAISS 和 pickle/json 索引文件，数据库约 9 MB。`.gitignore` 已忽略数据库、上传和索引，但物理文件仍存在于项目目录中；如果通过压缩包、网盘或手工复制提交，仍可能泄露个人简历或生成内容。

Phase 1 必须：

- 区分 `data/demo/` 与本地用户运行数据；
- 提供完全匿名的演示简历、职位和知识库种子；
- 提供可重复的数据库初始化和演示重置；
- 不把现有用户文件直接迁入演示数据；
- 在执行任何清理前先列出并确认精确目标，避免误删原始资料。

`frontend/node_modules` 也存在于项目目录中，虽然通常被忽略，但不应包含在最终作品集压缩包或仓库中。

## 11. 删除影响矩阵

| 准备删除的能力 | 直接影响 | 安全处理建议 |
| --- | --- | --- |
| 学习计划 | API、service、repository、model/schema、页面、类型、测试、Dify provider、职业助手个人上下文 | 先解除个人上下文引用；第一轮只取消注册和 UI，迁移表延后 |
| 求职信 | API、service、页面、类型、测试、Dify provider、共享 generated material | 不删除共享表和简历优化复用的事实校验工具 |
| 旧版面试 | API endpoint、service 方法、页面、路由、API client、类型和测试 | 先用聊天链路替换调用，再删除死代码 |
| 职位源管理 | Admin 路由/页面、适配器、同步服务、source 配置 | 保留职位表、职位技能和系统 source，先隐藏管理入口 |
| 多余申请状态 | 枚举、前后端筛选、状态迁移规则、历史数据 | 设计旧状态到五个目标状态的确定映射并补迁移测试 |

## 12. 风险清单

### P0：进入功能重构前必须处理

1. 缺少 README、环境示例和完整安装步骤，无法复现。
2. E2E 启动器模块路径错误；后端依赖环境缺失。
3. 运行数据可能包含个人文件，作品集发布前必须脱敏。
4. 旧版和新版面试并存，展示和维护边界不清。
5. 删除学习计划/职位源时存在隐藏依赖，不能直接批量删文件或删表。

### P1：影响项目可信度

1. AI Provider 与 Dify 应用过多，配置和错误处理不统一。
2. 模型调用日志存在但覆盖不完整，也没有面向用户的指标。
3. 现有评测不能完整证明技能抽取、风险识别和 RAG 引用质量。
4. 没有一条覆盖核心用户旅程的 E2E 测试。
5. 页面仍出现“阶段 4”等开发阶段文案，不适合作品集展示。

### P2：影响观感但不阻塞核心演示

1. 全局搜索、通知和账户设置是无实际功能的 UI。
2. 顶部标题对部分路由会回退成通用名称。
3. 移动端导航能力有限，整体更适合桌面演示。
4. 申请状态过多，降低主线清晰度。

代码搜索未发现明显的 `TODO`、`FIXME` 或业务 `NotImplementedError`。现有少量 `pass` 主要用于空异常类或有意忽略异常，并不能据此判断为未实现功能。真正的缺口集中在可复现性、重复链路、配置收敛和质量证明。

## 13. 推荐改造顺序

### Phase 1：先恢复“可交付、可复现”

1. 修复 E2E 和后端模块入口。
2. 建立并锁定 Python 3.12 环境，跑通后端测试。
3. 新增 `.env.example`、安装脚本、跨平台启动方式和健康检查。
4. 建立匿名演示数据与独立运行目录。
5. 新增项目 README 初版，明确 Mock 和真实 AI 两种模式。

### Phase 2：收敛功能边界

1. 移除学习计划和求职信入口及其非共享实现。
2. 删除旧版面试链路；验证后决定保留或隐藏聊天面试。
3. 移除复杂职位源管理和自动抓取入口，保留手工、CSV、演示职位。
4. 删除假搜索、假通知、不可用设置和阶段性文案。
5. 收敛申请状态。

### Phase 3：强化核心工程能力

1. 统一 AI Provider、结构化输出校验、重试、超时和错误映射。
2. 给全部模型调用补统一 usage log。
3. 完善匹配报告的技能证据、风险原因和改进建议。
4. 优化职业助手引用、无证据拒答和个人上下文边界。
5. 将简历优化嵌入匹配报告，弱化独立模块感。

### Phase 4：补质量证明

1. 建立 30–50 组匿名简历—职位评测集。
2. 计算技能抽取、required skill、blocking risk、结构化输出和推荐一致性指标。
3. 建立 RAG 检索、引用和拒答评测。
4. 新增核心旅程 E2E。
5. 在 Dashboard 增加模型调用次数、成功率、延迟、token 和失败原因视图。

### Phase 5：作品集包装

1. 完善 README：架构、业务流程、技术难点、运行方法、评测结果和演示账号。
2. 补 Docker/Compose 或同等可靠的一键启动方案。
3. 增加架构图、匹配流程图、关键页面截图和 3–5 分钟演示脚本。
4. 清理所有敏感数据、缓存、依赖目录和过时报告。
5. 准备简历项目描述与面试追问答案，所有指标只引用可复现实验结果。

## 14. Phase 0 验收结论

Phase 0 已完成代码、架构、功能、测试、配置、脚本、数据和交付风险审计。当前可以进入 Phase 1，但不建议先做视觉重构或新增功能；最先需要解决的是环境可复现、E2E 入口、后端测试、匿名演示数据和 README 基础。

未经下一次明确指令，不进入 Phase 1，也不删除任何现有功能或数据。
