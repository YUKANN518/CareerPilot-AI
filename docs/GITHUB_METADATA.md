# GitHub repository metadata

## Suggested description

Evidence-grounded AI job matching platform with explainable hybrid scoring, blocking-risk
detection, and real DeepSeek resume parsing.

## Suggested topics

`ai` `llm` `fastapi` `vue` `langgraph` `rag` `job-matching` `deepseek` `explainable-ai` `docker`

## Release notes

- Keep the repository public only after the final secret scan and tracked-file audit.
- Add a GitHub Actions badge only after the workflow has run successfully on the public repository.
- No remote is configured by this local packaging phase; repository creation and push require an
  explicit user-provided destination.
