import type { Page } from "@playwright/test"

/** Jobs now opens a single focused view containing all accessible target jobs. */
export async function showComplianceSnapshots(_page: Page): Promise<void> {
  void _page
  await Promise.resolve()
}
